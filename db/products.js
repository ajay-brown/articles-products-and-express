class DS_products {
  constructor() {
    this.list = list;
  }
  addNewProduct(name, price, inventory) {
    this.list.push({
      id: this.id, //updating id numbers
      name: newProdName,
      price: newProd.price,
      inventory: newProd.inventory
    });
  }
  getAllProducts() {
    return this.list;
  }
}
